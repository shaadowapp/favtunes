{
  "name": "firestore-rules-test",
  "version": "1.0.0",
  "description": "Firebase Firestore security rules testing",
  "main": "firestore-rules-test.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.3",
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testTimeout": 10000
  }
}